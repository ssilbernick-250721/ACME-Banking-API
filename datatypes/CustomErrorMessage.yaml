CustomErrorMessage:
  type: object
  properties:
    statusCode:
      type: string
    message:
      type: string
  required:
    - statusCode
    - message
  example:
    statusCode: 204 - No Content
    message: This method does not return a body in the response. Check the result of your action by sending a call to retrieve the resource.